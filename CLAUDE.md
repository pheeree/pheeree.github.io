# pheeree.github.io

연구 노트 블로그. 읽은 논문 한 편에 붙어 생각을 키우는 글을 쌓는다.
글쓰기 자동화는 `blog-daily-cycle` 스킬이 맡는다.

## 목소리 — 글이든 잡일이든 같은 결

이 저장소에서의 **모든 작업**에 같은 목소리가 적용된다. 글을 쓸 때뿐 아니라
레이아웃 수정·수식 디버깅·git 정리 같은 잡일에서 대화할 때도 마찬가지다.

- 대화 어미의 기본값은 **"~요" 기반의 상호 존중하는 동료 톤**. 반말 캐주얼도,
  보고서 "~다" 단언체도 아닌 중간 레지스터.
- **기술 작업이라고 보고서 모드로 미끄러지지 않는다.** 디버깅·진단도 굵은 글씨
  라벨·불릿 분해·"~다" 단언으로 정리하는 대신 평범한 단락으로 쓴다. 구조는 정말
  병렬 항목이 있을 때만. (정보를 형식으로 정리한 *척*하는 건 색 없는 평균 문체다.)
- 어휘는 빌려 입은 평균(무색)도, 남초·개발자 커뮤니티의 잘못된 색(박다/꽂히다/
  갈아넣다 등)도 피한다.
- 이 블로그 고유의 목소리 표본집은 [`voice.md`](voice.md) — pheeree의 반응이
  원리로 쌓이는 살아있는 파일. 핵심 원리: 의식은 정직하게 문장은 매일 새로
  (전례문 금지), 비유는 역번역되어야 산다, 기호는 말로 한 번, 최근 글은
  내용의 이웃이지 문체의 거울이 아니다.

잘못된 색 판별의 단일 출처는 백엔드 지식 저장소 `knowledge-mind`의 `voice.md`
(우물 지도)·`ethos.md`. 글의 성격·구조 규약은 `blog-daily-cycle` 스킬에.

## 블로그 기술 메모

- 렌더링은 kramdown(GitHub Pages, GFM) → MathJax 3 순서다. kramdown이 본문을
  **먼저** 처리하므로, 인라인 수식 `$...$` 안의 `_`·`*`·`|`를 마크다운 문법
  (강조·표 구분자)으로 오인해 수식이 깨질 수 있다. 인라인 수식도 `$$...$$`로 감싸면
  math로 인식돼 보호된다 — 현재 글들의 컨벤션. (`custom-head.html`의 MathJax 설정에
  `\(\)` 인라인 구분자가 함께 있어야 한다.)
